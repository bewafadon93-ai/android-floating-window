<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.floatingwindow">

    <!-- Required for drawing over other apps (API 23+) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Required to call startForeground() (API 28+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!--
        POST_NOTIFICATIONS: on Android 13+ (API 33+) users must grant this for
        notification badges and alerts. Foreground-service notifications are
        system-managed and still appear without this permission, but declaring
        it allows the system to ask the user at install time (API 33+).
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            FloatingWindowService:
            - Draws an overlay via TYPE_APPLICATION_OVERLAY (requires SYSTEM_ALERT_WINDOW)
            - Runs as a foreground service so the OS does not kill it in the background
            - Handles the STOP action from the persistent notification action button
        -->
        <service
            android:name=".FloatingWindowService"
            android:exported="false"
            android:enabled="true"
            android:stopWithTask="false" />

    </application>

</manifest>
